import bcrypt from 'bcrypt';
import { NextResponse } from 'next/server';
import { SESSION_DURATION_MS, SESSION_COOKIE_NAME, sessionCookieOptions } from '@/lib/auth';
import { query } from '@/lib/db';
type UserRow = { id: string; password_hash: string; force_password_change: boolean };
export async function POST(request: Request) { try { const { email, password } = await request.json(); if (typeof email !== 'string' || typeof password !== 'string') return NextResponse.json({ error: 'Credenciales incorrectas.' }, { status: 401 }); const userResult = await query<UserRow>('SELECT id, password_hash, force_password_change FROM users WHERE email = $1', [email.trim().toLowerCase()]); const user = userResult.rows[0]; const passwordMatches = user ? await bcrypt.compare(password, user.password_hash) : false; if (!user || !passwordMatches) return NextResponse.json({ error: 'Credenciales incorrectas.' }, { status: 401 }); const expiresAt = new Date(Date.now() + SESSION_DURATION_MS); const sessionResult = await query<{ id: string }>('INSERT INTO sessions (user_id, expires_at) VALUES ($1, $2) RETURNING id', [user.id, expiresAt]); const response = NextResponse.json({ force_password_change: user.force_password_change }); response.cookies.set(SESSION_COOKIE_NAME, sessionResult.rows[0].id, sessionCookieOptions(expiresAt)); return response; } catch { return NextResponse.json({ error: 'No fue posible iniciar sesión.' }, { status: 500 }); } }
